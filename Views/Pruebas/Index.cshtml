@model IEnumerable<WebApplication4.Models.Pruebas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Resultado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCandidato)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Resultado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCandidato)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idPruebas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idPruebas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idPruebas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
